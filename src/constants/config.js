const CONFIG = {
  DELAY_TIME_POLLING: 2000,
  NEW_NODE: "New element, not in the previous version!",
  NEW_FRAME: "New frame, absent in the previous version!",
  TIME_GAP_MS: 9 * 60 * 60 * 1000,
};

export default CONFIG;
